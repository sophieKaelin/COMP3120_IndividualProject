import React from "react"

const Feed = ({ feed }) => {
    //Will have many modes:
    //1. show all posts
    //2. show posts user is following
    //3. show posts by user
    //4. show posts mentioning user (HD)
    console.log("MODE IS: ", feed)
    return <h2>"HELLO WORLD"</h2>
}

export default Feed
